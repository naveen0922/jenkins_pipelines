pipeline {
    //agent any
    //agent { label 'slave1_ubuntu'}
    agent none

    stages {
        stage('STAGE1') {
            agent { label 'slave1_ubuntu'}
            steps {
                sh '''
                    sleep 5
                    echo "This is the step 1"
                '''
                echo "This is the step 1"
            }
        } 
        stage('BUILD') {   // ''' for multiple linux command in single block
            agent { label 'slave2_ubuntu'}
            steps { 
                echo "Building the application"                
                sh ''' 
                    #!/bin/bash
                    pwd
                    ls -lrt
                    sleep 5
                '''
                echo "This is the step 2"
            }
        }
    }
}